rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Partners & Clients logo lists + other CMS pages
    match /pages/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
